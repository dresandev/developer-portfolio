---
import Analytics from "@vercel/analytics/astro"
import { HOST_URL } from "astro:env/client"
import { getAlternateCanonicalLinks } from "@/helpers/get-alternate-canonical-links"
import Header from "@/components/header.astro"
import MoonMask from "@/components/icons/moon-mask.astro"
import Modals from "@/components/modals.astro"
import Footer from "@/components/footer.astro"
import "@/styles/reset.css"
import "@/styles/fonts.css"
import "@/styles/globals.css"
import "@/styles/tippy.css"

interface Props {
  title: string
  description?: string
  canonical?: string
  vh?: boolean
}

const { title, description, canonical, vh = false } = Astro.props

const locale = Astro.currentLocale
---

<!doctype html>
<html lang={locale}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{title}</title>

    <link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="shortcut icon" href="/favicon.ico" />

    <meta name="description" content={description} />

    {
      canonical && (
        <>
          <link rel="canonical" href={canonical} />
          {getAlternateCanonicalLinks(canonical).map(({ href, hreflang }) => (
            <link rel="alternate" hreflang={hreflang} href={href} />
          ))}
        </>
      )
    }

    <link
      rel="preload"
      href="/fonts/geist-vf.ttf"
      as="font"
      type="font/ttf"
      crossorigin="anonymous"
    />

    <link rel="sitemap" href="/sitemap-index.xml" />

    <meta property="og:url" content={HOST_URL} />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content={title} />
    <meta name="og:image" content=`${HOST_URL}/og.png` />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:locale" content="es_CO" />

    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content=`${HOST_URL}/og.png` />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:creator" content="@dresan_dev" />

    <script is:inline src="/scripts/theme.js"></script>
    <Analytics />
  </head>
  <body class:list={[{ vh }]}>
    <MoonMask />
    <Header />
    <main>
      <slot />
    </main>
    <Footer />
    <Modals />
  </body>
</html>

<script src="@/scripts"></script>
