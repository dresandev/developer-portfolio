export interface CoverProject {
	slug: string
	name: string
	imageUrl: string
	description: string
}

export interface Post {
	slug: string
	title: string
	description: string
	emoji: string
	publishedOn: string
}
